<!DOCTYPE html>
<html>
    <head>
<% include ../partials/head.ejs %>
    </head>
    <body>

        <% include ../partials/header.ejs%>

            <div class="welcome">
            <h1>Events</h1>
            <div><a href="/events/new" class="button-primary button-round button-shadow evbtn">Create New Event</a></div>


            <div class="cardcont">
            <% for (event of data){%>
                <div class="card">
       <h3 class="card-title"><%=event.title%></h3>
       <p><%=event.location%></p>
       <p><%=event.date%></p>
       <ul class="card-actions">
         <li><a href="/events/view/<%=event.id%>" class="button-primary evbtn">View</a></li>
         <li><a href="/events/edit/<%=event.id%>" class="button-primary evbtn">Edit</a></li>
         <li><a href="/events/delete/<%=event.id%>" class="button-primary evbtn">Delete</a></li>
       </ul>
        </div>
            <%}%>
            </div>
            *the view pages are shareable with the public!
        </div>

            <% include ../partials/footer.ejs%>
    </body>
</html>
